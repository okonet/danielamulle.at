{
  "name": "danielamulle.at",
  "version": "0.0.1",
  "private": true,
  "description": "danielamulle.at public website",
  "main": "",
  "scripts": {
    "start": "webpack-dev-server ",
    "clean": "rm -rf _build",
    "build": "NODE_ENV=production webpack -p",
    "deploy": "surge _build",
    "production": "npm run clean && npm run build && npm run deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Andrey Okonetchnikov",
  "license": "MIT",
  "dependencies": {
    "skrollr": "^0.6.26"
  },
  "devDependencies": {
    "autoprefixer-loader": "^1.0.0",
    "css-loader": "^0.9.0",
    "file-loader": "^0.8.1",
    "node-sass": "^3.1.2",
    "sass-loader": "^1.0.2",
    "static-site-generator-webpack-plugin": "^1.0.0",
    "style-loader": "^0.8.2",
    "surge": "^0.12.0",
    "url-loader": "^0.5.5",
    "webpack": "^1.4.13",
    "webpack-dev-server": "^1.6.6"
  }
}
